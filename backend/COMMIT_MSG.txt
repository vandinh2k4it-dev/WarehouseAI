chore(backend): dọn dependency thừa, sửa docstring nhầm nền tảng cũ, thêm script tự kiểm tra lệch schema.sql/models.py

Làm theo 3 việc "có thời gian thì làm thêm" đã đề xuất, xếp cùng đợt vì
đều nhỏ, không đụng logic nghiệp vụ nào.

1) requirements.txt — xoá `anthropic==0.40.*`
   Đã xác nhận KHÔNG còn import package này ở đâu trong code nữa (grep
   toàn bộ app/chatbot/ — chỉ còn nhắc tên "Claude/Anthropic" trong
   comment/docstring nói về việc ĐÃ chuyển sang Gemini, không có `import
   anthropic` thật nào). Thêm chú thích rõ ràng ở dòng `google-genai`
   rằng nó đã thay thế hoàn toàn anthropic, để không ai hiểu nhầm là còn
   dùng song song cả 2 API.
   Lợi ích: giảm 1 dependency khi cài lại môi trường từ đầu (hữu ích nếu
   cần dựng máy khác gấp trước ngày bảo vệ).

2) app/chatbot/mock_service.py — sửa docstring đầu file
   Docstring cũ vẫn ghi "dùng khi chưa có credit Claude API", "bật mock
   bằng cách không cấu hình ANTHROPIC_API_KEY" — trong khi code thật
   (app/chatbot/service.py) đã chuyển hẳn sang kiểm tra GEMINI_API_KEY từ
   lâu. Sửa lại đúng theo Gemini, thêm 1 câu ghi chú lý do (tránh ai đọc
   lại sau vài tuần bị nhầm tên biến môi trường cần set).

3) db/check_schema_sync.py — MỚI: script so cột schema.sql vs models.py
   Đây là việc "đáng cân nhắc trước bảo vệ" (không phải chỉ dọn dẹp) —
   nguyên nhân gốc của lỗi lệch schema.sql/models.py tuần trước (thiếu
   source_type, thiếu cột đếm-theo-dòng, thiếu hẳn bảng
   push_subscriptions) là vì 2 file này được duy trì SONG SONG BẰNG TAY,
   không có gì tự động phát hiện khi ai đó sửa models.py mà quên cập
   nhật schema.sql (hoặc ngược lại). Alembic (đã có sẵn trong
   requirements.txt) là giải pháp triệt để nhưng đổi hẳn cách quản lý
   schema — có thể quá rủi ro để đổi lúc gần bảo vệ. Script này là giải
   pháp tạm, AN TOÀN hơn: KHÔNG đổi cách chạy app, chỉ đọc 2 file có sẵn
   và báo lệch — chạy tay bằng:
       python db/check_schema_sync.py
   Không cần Postgres đang chạy, không cần DATABASE_URL đúng thật (chỉ
   parse tên cột, không kết nối DB thật).
   GIỚI HẠN ĐÃ GHI RÕ TRONG DOCSTRING: chỉ so TÊN CỘT (dùng regex thô cho
   schema.sql, SQLAlchemy metadata thật cho models.py) — KHÔNG kiểm tra
   kiểu dữ liệu, NULL/default, hay nội dung CHECK constraint có khớp
   không. Đủ để bắt đúng loại lỗi đã xảy ra tuần trước (thiếu hẳn 1 cột/
   1 bảng), không thay thế được việc tự đọc lại 2 file nếu chỉ đổi kiểu
   dữ liệu của cột đã có sẵn.
   ĐÃ TEST 2 CHIỀU:
   - Chạy trên schema.sql + models.py hiện tại (đã đồng bộ từ tuần
     trước) -> báo "✅ khớp cột" cho cả 9 bảng, không có false positive.
   - Cố tình xoá 1 dòng cột (`location VARCHAR(50)`) khỏi bản sao
     schema.sql để giả lập đúng loại lỗi đã xảy ra -> script bắt đúng,
     báo rõ "Có trong models.py, THIẾU trong schema.sql: ['location']"
     -> khôi phục lại bản gốc, chạy lại ra "✅ khớp cột" bình thường.
   Gợi ý dùng: chạy lệnh này 1 lần trước mỗi lần đóng gói deliverable có
   đụng tới models.py hoặc db/schema.sql, thành thói quen giống như
   esbuild/TestClient đã làm.
