fix(backend): đổi tên model Gemini — gemini-2.5-flash bị Google ngừng hỗ trợ cho user mới

Bối cảnh (bằng chứng thật, xác nhận NGAY tính hữu ích của bản vá chẩn
đoán MOCK vừa làm ở commit trước): thử hỏi chatbot ra lỗi 404 rõ ràng
ngay trong khung chat: "This model models/gemini-2.5-flash is no
longer available to new users. Please update your code to use
models/gemini-3.6-flash for the latest features and improvements."
— đúng như đã cảnh báo trong code, tên model AI của Google đổi rất
nhanh.

Cách sửa: đổi đúng 1 dòng MODEL_NAME từ "gemini-2.5-flash" sang
"gemini-3.6-flash" — ĐÚNG THEO CHỈ DẪN CỦA GOOGLE trong chính thông
báo lỗi, không phải tự đoán tên model khác. Cập nhật lại comment đầu
file ghi rõ lịch sử đổi model này, kèm hướng dẫn: lần sau gặp lỗi 404
tương tự, đọc kỹ nội dung lỗi vì Google thường TỰ NÓI LUÔN tên model
mới cần đổi sang ngay trong thông báo.

Không cần test lại toàn bộ logic (đã test kỹ ở commit trước, chỉ đổi
đúng 1 giá trị string, không đổi cấu trúc code) — chỉ xác nhận cú
pháp hợp lệ.

Việc cần làm: push xong thử hỏi lại chatbot — nếu 3.6-flash cũng bị
đổi/ngừng hỗ trợ trong tương lai, lặp lại đúng cách này (đọc lỗi 404,
lấy tên model Google chỉ định, đổi MODEL_NAME).
