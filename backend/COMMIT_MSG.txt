fix(backend): thêm log chẩn đoán cho bước tìm video đã vẽ khung hộp — đang bị nuốt lỗi im lặng

Bối cảnh:
Đếm video đã ra đúng số (34) sau khi sửa CARTON_MODEL_PATH, nhưng
KHÔNG thấy khung video nào hiện trên kết quả (tính năng "xem lại video
đã vẽ khung hộp" thêm ở commit trước). Nguyên nhân thật chưa xác định
được vì bước tìm/chuyển file được bọc trong try/except NUỐT LỖI HOÀN
TOÀN (except Exception: pass) — annotated_video_url âm thầm về None,
không có cách nào biết được lý do thật (thư mục không tồn tại? sai
tên file ultralytics đặt? quyền ghi?).

Thay đổi (app/routers/camera.py, endpoint /count-video):
- In ra Railway logs (xem được qua tab Deployments > Logs, hoặc lệnh
  `railway logs`) đầy đủ từng bước:
  + Đường dẫn tuyệt đối thư mục đang tìm.
  + Thư mục đó có tồn tại không.
  + TOÀN BỘ tên file thật sự có trong thư mục (nếu thư mục tồn tại) —
    để biết chính xác ultralytics đặt tên/đuôi file gì, so với giả
    định *.mp4/*.avi đang tìm.
  + Số file .mp4/.avi khớp được.
  + Nếu lỗi exception xảy ra, in ra ĐÚNG nội dung lỗi thật thay vì
    nuốt hoàn toàn như trước.

Việc cần làm: chạy lại upload video 1 lần nữa, sau đó vào Railway ->
tab Deployments -> Logs (hoặc chạy `railway logs` qua CLI đã cài từ
trước), tìm các dòng bắt đầu bằng "[annotated-video]" — copy gửi lại,
mình đọc log thật sẽ biết chính xác lý do và sửa đúng chỗ (nhiều khả
năng ultralytics đặt tên/đuôi file khác giả định, hoặc cấu trúc thư
mục project/name khác dự đoán khi dùng stream=True).
