fix(backend): seed_main.py commit sau mỗi phiếu — tránh mất công làm lại khi mạng chập chờn

Bối cảnh thật: chạy seed qua đường hầm SSH (railway connect
--tunnel-only) bị rớt giữa chừng với lỗi "SSL error: unexpected eof
while reading" ngay khi đang tạo phiếu PN-2026-003 — do TOÀN BỘ 7
phiếu trước đó gộp chung 1 giao dịch (chỉ có đúng 1 db.commit() sau
khi tạo xong tất cả), khiến kết nối phải giữ liên tục qua nhiều câu
lệnh SQL, dễ bị đường hầm ngắt giữa chừng hơn.

Cách sửa: thêm db.commit() ngay sau MỖI phiếu được tạo (7 lần commit
riêng biệt thay vì 1 lần duy nhất ở cuối) — nếu mạng rớt giữa chừng,
chỉ mất đúng phiếu đang làm dở, các phiếu trước đó đã lưu thật, chạy
lại (mặc định wipe=True) vẫn an toàn dù có tốn công làm lại từ đầu,
nhưng RỦI RO RỚT GIỮA CHỪNG giảm hẳn vì mỗi giao dịch giờ ngắn hơn
nhiều.

Đã test lại toàn bộ bằng SQLite local — kết quả tóm tắt giống hệt
bản trước (30 sản phẩm, 54 lô, 7 phiếu, 5 lượt xuất, 16 cảnh báo) —
xác nhận việc chia nhỏ giao dịch không làm thay đổi logic/kết quả.
