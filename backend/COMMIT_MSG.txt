feat(backend): thêm tham số conf cho endpoint /count-video — chỉnh được độ nhạy khi Quay video

Bối cảnh (bằng chứng thật từ ảnh chụp): sau khi có bộ lọc diện tích
(commit trước), 1 trường hợp nhận nhầm vẫn lọt qua (id:57 carton_box
0.56 — tấm cửa cuốn/vách, chỉ chiếm ~20-25% khung hình, dưới xa ngưỡng
65% nên không bị lọc). Quan sát: độ tin cậy của khung sai này (0.56)
THẤP HƠN rõ rệt so với khung đúng thật (0.85-0.90) — gợi ý dùng
confidence threshold làm tín hiệu bổ sung, tách biệt với bộ lọc diện
tích đã có.

Thay đổi (app/routers/camera.py, endpoint /count-video):
- Thêm tham số form conf (mặc định 0.5, khớp mặc định gốc của
  count_boxes_in_video) — cho phép frontend GỬI KÈM ngưỡng confidence
  mong muốn thay vì luôn dùng cứng giá trị mặc định.
- Truyền thẳng vào count_boxes_in_video(conf=conf, ...) — hàm này đã
  có sẵn tham số conf từ trước, chỉ chưa từng được expose ra API.
- Ghi rõ trong docstring: tăng ngưỡng giúp lọc bớt nhận nhầm có độ tin
  cậy thấp, NHƯNG không loại được hết mọi trường hợp — đã từng quan
  sát có case nhận nhầm (tủ máy công nghiệp) lên tới 0.83, ngang khung
  đúng, để không đặt kỳ vọng sai là tính năng này giải quyết triệt để.

ĐÃ TỰ KIỂM TRA: cú pháp Python hợp lệ (ast.parse thành công), Form đã
có sẵn trong import (dùng chung với threshold_pct đã có từ trước,
không cần thêm import mới).

Việc cần làm: cần đi kèm bản sửa frontend (api.js + CountingScreen.jsx,
gửi cùng đợt) — thiếu 1 trong 2 phía thì tham số conf sẽ không có tác
dụng (backend nhận nhưng frontend không gửi, hoặc ngược lại).
