feat(dashboard): thêm Dashboard báo cáo/phân tích — Nhóm B (mục an toàn nhất)

Chỉ chọn 1 hạng mục an toàn nhất trong Nhóm B (đã trao đổi trước) — KHÔNG
đổi schema database, KHÔNG đụng tới luồng nhập/xuất/đếm camera đang chạy
ổn định. Tận dụng lại đúng dữ liệu InventoryTransaction/Inventory/Alert
đã có sẵn từ đầu.

BACKEND (thêm mới, không sửa logic cũ):
- app/schemas.py: thêm DailyFlowPoint, TopProductMovement, AnalyticsKpis,
  AnalyticsOut.
- app/routers/inventory.py: thêm GET /inventory/analytics?days=N — trả
  về (1) nhập/xuất theo từng ngày, (2) top 10 sản phẩm quay vòng nhiều
  nhất (tổng nhập+xuất), (3) 4 KPI nhanh (cảnh báo mở, sản phẩm sắp hết,
  lô sắp hết hạn, tổng số giao dịch trong khoảng ngày xem).
  ĐÃ TEST THẬT logic query bằng SQLite in-memory (không chỉ kiểm tra cú
  pháp) — xác nhận số liệu tính đúng với dữ liệu giả lập.