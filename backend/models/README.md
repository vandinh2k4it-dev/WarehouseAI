Đặt file `carton_counter_best.pt` (model YOLOv8 đã train, tải từ Kaggle
Output) vào đúng thư mục này. File này KHÔNG có sẵn trong bản đóng gói
gửi qua Claude vì mình (Claude) chưa từng nhận được file model thật —
bạn tải trực tiếp từ Kaggle rồi copy vào đây, sau đó commit vào git
(dung lượng ~21.5MB, không cần Git LFS).
