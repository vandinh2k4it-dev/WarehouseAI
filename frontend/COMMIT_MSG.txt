fix(frontend): hiện đúng mã phiếu xuất (PX0001) thay vì số phiên thô

Nối theo backend cùng đợt (xem COMMIT_MSG.txt bên backend) — backend
giờ tự sinh session_code dạng "PX0001" cho mỗi phiên đếm xuất, đại
diện luôn cho mã phiếu xuất kho.

- src/components/PrintExportReceipt.jsx: đổi hiển thị từ "Số phiên
  đếm: #88" (raw session.id) sang "Số phiếu xuất: PX0001"
  (session.session_code) — có fallback về "#id" nếu session_code cũ
  chưa có (dữ liệu tạo trước khi có bản vá backend này, tránh hiện
  "undefined" trên các phiên cũ).

Đã kiểm tra cú pháp (esbuild), không cần test lại toàn bộ luồng vì
chỉ đổi đúng 1 biểu thức hiển thị, không đổi logic gọi API/tính toán.
