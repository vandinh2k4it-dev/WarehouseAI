fix(frontend): hiện nút "Đếm lại" cho dòng bị kẹt "đang đếm" (thoát app giữa chừng)

Nối vào backend đã có sẵn cơ chế xử lý (xem COMMIT_MSG.txt bên backend
cùng đợt) — trước đây dù backend có cho phép đếm lại, giao diện vẫn
KHÔNG HIỆN nút nào cho trạng thái 'counting', nên người dùng vẫn kẹt
dù backend đã sẵn sàng.

File sửa:
- src/pages/ImportFlow.jsx
  + Đổi nhãn trạng thái 'counting' từ "đang đếm" -> "đang đếm dở" (rõ
    nghĩa hơn — phân biệt với đang đếm THẬT SỰ ngay lúc đó).
  + Thêm điều kiện hiện nút bấm: ngoài 'not_started' và
    'needs_review' như trước, giờ 'counting' cũng hiện nút — nhãn nút
    đổi thành "Đếm lại" (thay vì "Đếm") để rõ ý đây là khởi động lại
    phiên bị bỏ dở, không phải bắt đầu lần đầu.

ĐÃ TỰ KIỂM TRA (kết hợp cùng test backend — xem chi tiết đầy đủ bên
COMMIT_MSG.txt backend, không lặp lại ở đây): xác nhận toàn bộ chuỗi
từ bấm nút -> gọi API -> tạo phiên mới -> phiên cũ tự đóng đều hoạt
động đúng qua TestClient thật.
