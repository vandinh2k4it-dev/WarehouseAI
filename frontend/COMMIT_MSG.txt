feat(frontend): tách Nhập/Xuất kho riêng biệt + Lịch sử xuất kho + nối lại Chatbot AI

Bối cảnh: người dùng phát hiện 2 lỗ hổng thật — (1) Xuất kho không có
lịch sử/phiếu để xem lại, khác Nhập kho; (2) Chatbot đã làm xong từ
trước (mục 6.6 đề cương) nhưng chỉ tồn tại ở bản demo desktop cũ
(static/index.html), CHƯA TỪNG được nối vào React PWA khi chuyển kiến
trúc — bị "lạc mất" giữa chừng, không phải chưa làm.

============================================================
1. Tách sidebar — Nhập kho / Xuất kho / Trợ lý AI riêng biệt
============================================================
- src/components/TopNav.jsx: TABS đổi từ 4 mục (gộp "Nhập/Xuất kho")
  thành 6 mục riêng: Tổng quan, Sản phẩm, Nhập kho, Xuất kho, Trợ lý
  AI, Cảnh báo — bỏ hẳn route trung gian /inout.
- src/App.jsx: xoá import/route InOutChoice, thêm route
  /export/history và /chatbot. File src/pages/InOutChoice.jsx không
  còn được route tới nhưng CHƯA XOÁ file (để nguyên, không ảnh hưởng
  gì nếu không dùng tới — an toàn hơn xoá nhầm nếu còn chỗ khác tham
  chiếu mà mình chưa thấy).
- src/pages/ExportFlow.jsx, ImportFlow.jsx: bỏ link "← Chọn Nhập/Xuất
  kho" trỏ về /inout (route không còn tồn tại) — Xuất kho thêm nút
  "🕘 Lịch sử xuất kho" ở đầu trang.

============================================================
2. Lịch sử xuất kho (trang mới)
============================================================
- src/pages/ExportHistory.jsx (route mới /export/history)
  Bảng chi tiết từng lượt xuất (thời gian, sản phẩm, lô, số lượng,
  nguồn "Gõ tay"/"Qua camera", ghi chú) — dùng lại ĐÚNG style bảng
  dashboard đã có (.adminTable, .tableScroll) cho nhất quán với trang
  Sản phẩm. Có 2 thẻ tổng hợp phía trên (số lượt xuất, tổng số lượng)
  + ô tìm kiếm theo tên sản phẩm.
- src/api.js: thêm listExportHistory() — gọi endpoint mới bên backend.

============================================================
3. Chatbot AI (trang mới, nối lại)
============================================================
- src/pages/Chatbot.jsx (route mới /chatbot)
  Giao diện chat: bong bóng hội thoại (người dùng phải/màu cam, bot
  trái/trung tính), 3 câu hỏi gợi ý khi chưa có hội thoại, hiệu ứng
  "đang gõ" (3 chấm nhấp nháy) trong lúc chờ, Enter để gửi (Shift+Enter
  xuống dòng). Tự giữ "history" trả về từ mỗi lần gọi và gửi lại nguyên
  vẹn ở lượt hỏi tiếp theo — không tự diễn giải cấu trúc bên trong của
  history (thuộc về backend quản lý), chỉ đơn giản lưu và gửi lại.
- src/api.js: thêm chatbotAsk(message, history).
- src/styles/app.css: thêm toàn bộ CSS mới cho chatbot (.chatbot-*).

ĐÃ TỰ KIỂM TRA TÍCH HỢP ĐẦY ĐỦ (không chỉ test riêng lẻ từng file):
Do sandbox thiếu 1 số trang khác (Overview, Alerts, ScanReceipt,
CountingScreen) để chạy được Vite thật, đã dùng esbuild BUNDLE TRỰC
TIẾP các file THẬT (App.jsx, TopNav.jsx, ExportFlow.jsx, ImportFlow.jsx,
ExportHistory.jsx, Chatbot.jsx, api.js — không phải chép lại code vào
môi trường test) thành 1 file ESM, viết shim ES module nối UMD
React/ReactDOM/ReactRouterDOM (cài qua npm, không qua CDN) với import
map, rồi chạy Playwright với API giả lập qua route interception:
- Sidebar hiện ĐÚNG ĐỦ 6 mục mới, đúng thứ tự, badge cảnh báo đúng vị trí.
- Điều hướng THẬT (không giả lập): bấm "Xuất kho" -> tab sáng đúng,
  bấm "Lịch sử xuất kho" -> chuyển đúng trang, hiện đúng dữ liệu giả
  lập (2 lượt xuất, TỔNG SỐ LƯỢNG TÍNH ĐÚNG 15+8=23 — xác nhận phép
  cộng thật, không phải số cứng), đúng nhãn "Gõ tay"/"Qua camera".
- Chatbot: bấm câu hỏi gợi ý -> gọi API thật (mock) -> hiện đúng bong
  bóng user (phải) + bong bóng bot (trái) với ĐÚNG NỘI DUNG trả về từ
  API, xác nhận bằng cách kiểm tra text thật xuất hiện trong DOM.
- Đã dọn sạch 4 file stub tạm dùng để build test (Overview.jsx,
  Alerts.jsx, ScanReceipt.jsx, CountingScreen.jsx) trước khi đóng gói
  — xác nhận qua ls rằng chỉ còn đúng các file thật trong thư mục
  trước khi zip, không lẫn file giả vào bản giao.

CHƯA làm/cần lưu ý:
- src/pages/InOutChoice.jsx còn tồn tại nhưng không còn route nào trỏ
  tới — có thể xoá hẳn nếu chắc chắn không dùng lại, hoặc giữ nguyên
  không ảnh hưởng gì (không import ở đâu khác nên an toàn khi build).
- Route /products/unmapped (trang "Sản phẩm chưa gán" làm ở đợt trước
  đó) VẪN CHƯA có trong App.jsx lần này — đã nhắc ở commit trước,
  nhắc lại lần nữa vì vẫn chưa được khôi phục, bạn tự quyết định có
  cần thêm lại không.
- Chưa nối Toast.jsx/ConfirmDialog.jsx (đã làm ở đợt trước) vào các
  trang mới này — ExportHistory.jsx/Chatbot.jsx dùng cách báo lỗi cũ
  (div màu đỏ) cho nhất quán với các trang khác hiện tại, chưa đồng
  loạt chuyển sang Toast vì cần rà soát TOÀN BỘ app 1 lượt, ngoài
  phạm vi yêu cầu lần này.
