fix(frontend): tinh gọn thẻ cảnh báo ở Tổng quan — rút gọn 1 dòng, bấm "Xem thêm" mới hiện chi tiết

Bối cảnh: mỗi cảnh báo hiện TOÀN BỘ nội dung kỹ thuật cùng lúc (ngưỡng
%, số phiên, dòng "cần kiểm tra lại"...) khiến mỗi dòng cảnh báo dài
3-4 dòng, làm trang Tổng quan nhìn rối — đúng phản ánh thật từ ảnh
chụp người dùng gửi.

Cách sửa (src/pages/Overview.jsx):
- Rút gọn message về đúng CÂU ĐẦU TIÊN (cắt tới dấu ". " đầu tiên tìm
  thấy trong chuỗi) cho cái nhìn mặc định — phần chi tiết kỹ thuật
  phía sau (thường bắt đầu bằng "Chưa cập nhật tồn kho... cần kiểm
  tra lại (phiên #N)") bị ẩn đi cho tới khi bấm "Xem thêm".
- Thêm state expandedAlertId (id của ĐÚNG 1 cảnh báo đang mở rộng —
  các cảnh báo khác không bị ảnh hưởng, độc lập với nhau).
- Chỉ hiện nút "Xem thêm/Thu gọn" khi message THỰC SỰ có phần bị cắt
  (so sánh độ dài shortMsg với message gốc) — cảnh báo nào vốn đã
  ngắn (như low_stock) thì không hiện nút thừa không cần thiết.
- Cách làm CHUNG cho MỌI loại cảnh báo (discrepancy/low_stock/
  expiring_soon) bằng cách cắt theo dấu câu, không cần biết cấu trúc
  chi tiết bên trong từng loại message khác nhau — đơn giản, không
  cần đổi gì ở backend hay parse theo alert_type riêng.

File sửa:
- src/styles/app.css — thêm .alertRow-clickable (con trỏ tay + không
  đẩy vỡ layout khi nội dung dài/ngắn khác nhau) và .alertRow-toggle
  (chữ "Xem thêm"/"Thu gọn" màu cam, phân biệt rõ là có thể bấm).

ĐÃ TỰ KIỂM TRA (esbuild bundle CHÍNH FILE THẬT + Playwright, dùng
ĐÚNG NỘI DUNG MESSAGE THẬT lấy từ ảnh chụp màn hình gốc — không phải
message tự bịa ra để test):
- Xác nhận KHÔNG CÒN chữ "None" nào trong toàn bộ trang (kiểm tra
  bằng cách tìm chuỗi "None" trong text nội dung thật của trang,
  không phải đoán bằng mắt) — xác nhận đồng thời cả bản vá backend
  (tên sản phẩm đúng) VÀ bản vá frontend (rút gọn) hoạt động đúng
  cùng lúc.
- Bấm "Xem thêm" ở đúng 1 cảnh báo -> CHỈ cảnh báo đó mở rộng ra
  (hiện đủ "Chưa cập nhật tồn kho... phiên #79"), đổi chữ thành "Thu
  gọn" — cảnh báo khác vẫn giữ nguyên trạng thái gọn, xác nhận state
  độc lập theo từng id, không bị lẫn giữa các dòng.
- npm run build (qua esbuild) thành công, không lỗi cú pháp.
