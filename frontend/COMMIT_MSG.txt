fix(frontend): hiện lỗi thật khi đếm trực tiếp thất bại liên tục — trước đây im lặng đứng số 0

Bối cảnh lỗi (báo cáo thật từ người dùng):
Test đếm trực tiếp trên video/camera từng chạy đúng trước đây (thời
CLI count_pipeline.py, chưa deploy) — nhưng qua PWA mới lại ra đúng
0, không nhích lên chút nào.

Nguyên nhân của việc KHÔNG THẤY LỖI (bug do chính mình gây ra khi
viết LiveCountingScreen.jsx lần trước):
Code cũ xử lý lỗi mỗi khung hình theo kiểu "im lặng bỏ qua hoàn
toàn" (catch rỗng, không set state gì) — chủ đích là để 1 lần lỗi
thoáng qua do mạng chập chờn không làm phiền người dùng. Nhưng hậu
quả phụ: nếu MỌI khung hình đều lỗi liên tục (vd sai đường dẫn
model, thiếu thư viện, model chưa load được trên server) thì người
dùng KHÔNG BAO GIỜ biết được — chỉ thấy số đếm đứng yên ở 0 mãi,
trông như model không nhận diện được gì trong khi thật ra server
đang trả lỗi 500 liên tục ở phía sau, chưa từng chạy được 1 khung
nào thành công.

Cách sửa (src/components/LiveCountingScreen.jsx):
- Đếm số lần lỗi LIÊN TIẾP (consecutiveFailsRef). Lỗi 1-2 lần liên
  tiếp vẫn im lặng bỏ qua như cũ (mạng chập chờn thoáng qua, không
  đáng báo động). Từ lần thứ 3 liên tiếp trở lên -> hiện hẳn banner
  đỏ đè lên góc dưới video, với ĐÚNG nội dung lỗi thật lấy từ
  response.detail của backend (không phải câu chung chung).
- Lỗi tự RESET về 0 ngay khi có 1 khung xử lý thành công — nếu chỉ
  là lỗi thoáng qua rồi tự phục hồi, banner tự biến mất, không cần
  người dùng bấm tắt gì.
- Bắt cả 2 loại lỗi: response không phải 200 (đọc .detail từ JSON
  trả về) VÀ lỗi mạng/exception khi gọi fetch (mất kết nối hẳn).

ĐÃ TỰ KIỂM TRA (Playwright + camera ảo thật, KHÔNG chặn giả lập
request lần này — để nó tự gọi thật vào backend thật đang thiếu
ultralytics/opencv trong sandbox, đúng mô phỏng tình huống "mọi khung
đều lỗi" người dùng gặp phải):
- Sau ~3.5 giây (5 lần thử, mỗi 700ms), banner đỏ hiện đúng nội dung
  lỗi thật: "Server lỗi khi xử lý khung hình: Xử lý khung hình thất
  bại: ModuleNotFoundError('No module named cv2')" — xác nhận cơ chế
  hiện lỗi hoạt động đúng, người dùng giờ THẤY ĐƯỢC nguyên nhân thật
  thay vì đoán mò tại sao số cứ đứng ở 0.
- npm run build thành công, không lỗi.

Gợi ý chẩn đoán cho người dùng (không phải sửa trong code này — ghi
lại để nhớ hướng debug tiếp nếu banner lỗi hiện ra khi test thật trên
Railway):
- Nếu lỗi là "No module named cv2"/"ultralytics" -> deploy Railway
  thiếu thư viện, kiểm tra lại requirements.txt đã có ultralytics
  chưa, và RAILPACK_BUILD_APT_PACKAGES/RAILPACK_DEPLOY_APT_PACKAGES
  đã đặt đúng (libgl1...) chưa.
- Nếu lỗi liên quan tới đường dẫn file model (FileNotFoundError...)
  -> kiểm tra lại CARTON_MODEL_PATH trên Railway Variables có trỏ
  đúng file models/carton_counter_best.pt đã commit vào repo chưa.
