fix(frontend): đổi màu khung hộp nhận diện — cam cũ khó thấy trên thùng carton (cũng màu cam/nâu)

Bối cảnh (ảnh chụp thật): khung hộp + chữ nhãn dùng màu cam accent
(#F5A524) của giao diện — nhưng thùng carton THẬT cũng màu nâu/cam,
khiến khung hộp gần như lẫn vào chính vật thể đang khoanh vùng, rất
khó nhìn khi demo.

Cách sửa (src/components/LiveCountingScreen.jsx, hàm drawBoxes):
- Đổi màu viền khung + chữ nhãn từ cam (#F5A524) sang xanh lá/cyan
  (#00E676) — tương phản mạnh với tông màu nâu/cam/be của thùng
  carton thật, đồng thời cũng là màu phổ biến trong các demo nhận
  diện vật thể (dễ nhận biết, quen mắt).
- Thêm NỀN ĐẶC phía sau chữ nhãn (trước đây chữ vẽ trực tiếp lên nền
  video, dễ bị lẫn nếu nền sáng/trùng màu) — giờ luôn có 1 khối màu
  xanh lá đặc phía sau, chữ đen phía trên — đọc được rõ ràng bất kể
  nền video là gì, đúng kiểu nhãn trong ảnh demo COCO tham khảo.
- Dùng ctx.measureText() để tính đúng độ rộng nền theo đúng độ dài
  chữ track ID (không cố định cứng, số ID dài/ngắn khác nhau).

ĐÃ TỰ KIỂM TRA (Playwright + camera ảo thật, chặn response giả lập
để có toạ độ khung hộp cố định kiểm tra được):
- Route giả lập /live-frame được gọi đúng (xác nhận qua đếm số lần
  gọi thật, không phải suy đoán).
- Ảnh chụp màn hình xác nhận trực quan: khung hộp hiện đúng màu xanh
  lá/cyan mới, không còn màu cam cũ dễ lẫn vào thùng.
- npm run build thành công, không lỗi.
