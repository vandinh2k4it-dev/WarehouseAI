feat(frontend): thêm Toast + hộp xác nhận dùng chung, chuẩn hoá đổ bóng/bo góc toàn app

Bối cảnh: người dùng chia sẻ 1 bộ nguyên tắc thiết kế chi tiết từ 1 dự
án KHÁC (dùng Tailwind CSS, BottomNav, hệ màu slate/indigo/emerald/
rose/amber, sáng+tối...) và muốn áp dụng cho app này. Quyết định có
chủ đích SAU KHI TRAO ĐỔI RÕ với người dùng: KHÔNG chuyển sang
Tailwind (rủi ro rất cao — cần viết lại toàn bộ 10+ trang/component,
đổi cấu trúc điều hướng, thêm build tooling mới, mà sandbox lúc này
không có đủ TOÀN BỘ file gốc để làm an toàn/kiểm chứng từng trang) —
thay vào đó ÁP DỤNG ĐÚNG TINH THẦN cốt lõi ("nhất quán tuyệt đối" theo
đúng lời người dùng tự kết luận) trong CSS thuần đã có sẵn, không đổi
công nghệ, giảm rủi ro vỡ các trang chưa có file để kiểm tra.

File mới:
- src/components/Toast.jsx
  Hệ thống thông báo dùng chung qua React Context — bọc <ToastProvider>
  1 lần ở App.jsx, mọi trang con gọi useToast() để hiện thông báo
  thành công/lỗi/info, tự biến mất sau 3 giây, bấm vào tắt sớm được.
  Trước đây mỗi trang tự hiện kết quả theo cách khác nhau (số trong
  <div>, không nhất quán) — giờ có 1 điểm chuẩn duy nhất.

- src/components/ConfirmDialog.jsx
  Hộp xác nhận dùng chung trước các hành động quan trọng/khó hoàn tác
  — che mờ nền, đóng được bằng cách bấm ra ngoài HOẶC bấm nút Huỷ,
  có biến thể "danger" (nút đỏ) cho hành động nguy hiểm.

File sửa:
- src/styles/app.css
  + Thêm biến --shadow-sm/md/lg, --radius-sm/lg (khai báo bổ sung
    vào :root, KHÔNG sửa index.css có sẵn — CSS tự gộp biến từ nhiều
    file cùng nhắm :root, không xung đột với biến màu đã có).
  + Áp dụng lại .card, .statCard dùng đúng var(--shadow-md) thay vì
    tự viết rgba() riêng lẻ — 1 công thức đổ bóng chuẩn dùng lại,
    đúng tinh thần "lặp lại có kỷ luật" từ bộ nguyên tắc được chia sẻ.
  + CSS đầy đủ cho .toastStack/.toast (+3 biến thể success/error/info)
    và .confirmOverlay/.confirmBox/.dangerBtn.

ĐÃ TỰ KIỂM TRA KỸ (dựng môi trường test React thật lần 2, do sandbox
vẫn thiếu App.jsx để wiring ToastProvider thật vào cây component):
- esbuild xác nhận cú pháp JSX + CSS hợp lệ cho cả 3 file.
- Test Playwright với DEMO APP THẬT (ToastProvider bọc component con
  gọi useToast() qua Context — đúng cơ chế thật, không phải giả lập):
  + Toast hiện đúng, đúng màu/icon theo loại (thành công/lỗi/info).
  + NHIỀU toast xếp chồng đúng thứ tự thời gian (mới nhất dưới cùng).
  + Toast TỰ BIẾN MẤT đúng sau 3 giây (đếm số phần tử .toast còn lại
    sau 3.2s, xác nhận bằng 0 — không phải đoán).
  + Hộp xác nhận: bấm nút "Xác nhận" đóng đúng dialog (xác nhận bằng
    cách đếm .confirmOverlay còn lại = 0).
  + Hộp xác nhận: bấm RA NGOÀI overlay cũng đóng đúng (không bắt buộc
    phải bấm đúng nút Huỷ) — xác nhận UX thân thiện hoạt động đúng.

Việc cần làm để thực sự dùng được (chưa làm trong lần này vì thiếu
file App.jsx):
1. Trong App.jsx, import và bọc ToastProvider quanh toàn bộ
   HashRouter (hoặc tương đương).
2. Thay dần các chỗ hiện kết quả rời rạc hiện tại (vd trong
   ImportFlow.jsx, CountingScreen.jsx, Alerts.jsx...) bằng
   useToast() — CHƯA làm trong lần này vì không có các file đó, chỉ
   mới tạo xong 2 component sẵn sàng dùng, chưa nối vào các trang cụ
   thể. Gửi các file đó ở lượt sau nếu muốn mình nối tiếp.
