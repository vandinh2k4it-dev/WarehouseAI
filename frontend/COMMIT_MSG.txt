fix(frontend): sửa nút "Quét phiếu mới" tràn màn hình mobile + rút gọn placeholder chatbot

Bối cảnh: sau khi thêm nút "Tạo phiếu tay" cạnh "Quét phiếu mới" ở
đợt trước, 2 nút + tiêu đề "Chọn phiếu nhập" không đủ chỗ trên 1 hàng
ở màn mobile hẹp — nút "Quét phiếu mới" bị tràn hẳn ra ngoài mép phải
màn hình.

Cách sửa (src/styles/app.css):
.card-headRow thêm flex-wrap:wrap — tiêu đề và cụm nút tự xuống dòng
khi không đủ chỗ, thay vì tràn ra ngoài. Thêm flex-wrap:wrap cho cụm
div chứa 2 nút bên trong luôn, phòng màn cực hẹp vẫn không đủ chỗ cho
cả 2 nút cùng hàng.

ĐÃ TỰ KIỂM TRA bằng đo lường thật (Playwright, viewport 390px):
- TRƯỚC khi sửa: scrollWidth=421px > clientWidth=390px — xác nhận
  tràn ngang thật 31px.
- SAU khi sửa: scrollWidth=390px = clientWidth=390px — hết tràn hoàn
  toàn. Ảnh chụp xác nhận tiêu đề xuống dòng riêng, 2 nút xuống hàng
  dưới, hiển thị trọn vẹn cả 2 chữ.

Cách sửa 2 (src/pages/Chatbot.jsx):
Rút gọn placeholder ô nhập chatbot từ "Nhập câu hỏi… (Enter để gửi,
Shift+Enter xuống dòng)" thành "Nhập câu hỏi cho trợ lý" — theo đúng
yêu cầu, chữ ngắn gọn hơn, đỡ chiếm chỗ trên màn hẹp.
