feat(frontend): trang Sản phẩm thành dashboard quản trị chuyên nghiệp — bảng chi tiết + % + biểu đồ cột

Bối cảnh: thay thế lưới thẻ (card grid) cũ bằng bảng dữ liệu chi tiết
kiểu dashboard quản trị thật (giống phần mềm quản lý kho chuyên
nghiệp) — đầy đủ cột số liệu, cột phần trăm đóng góp tồn kho, kèm
biểu đồ cột trực quan top sản phẩm.

File sửa:
- src/pages/Products.jsx (viết lại phần hiển thị, giữ nguyên toàn bộ
  logic lấy dữ liệu/tính toán/sắp xếp/tìm kiếm đã có từ trước)
  + THÊM: tính totalStockAll (tổng tồn kho toàn bộ sản phẩm cộng lại)
    — dùng làm mẫu số tính % đóng góp của từng sản phẩm.
  + THÊM: tính topForChart (8 sản phẩm tồn kho cao nhất) — dữ liệu
    cho biểu đồ cột.
  + THÊM: 2 lựa chọn sắp xếp mới "Tồn kho cao nhất/thấp nhất trước".
  + THAY: bỏ hẳn lưới thẻ (.productGrid/.productCard), thay bằng
    <table> với 7 cột: #, Sản phẩm (kèm SKU), Tồn kho, % Tổng kho
    (thanh tiến trình mini + số %), Trạng thái (badge màu theo 3 mức:
    Đủ hàng/Sắp hết/Hết hàng), Số lô, HSD gần nhất.
  + THÊM: bấm vào 1 dòng để MỞ RỘNG xem chi tiết từng lô hàng của
    đúng sản phẩm đó (thay vì luôn hiện sẵn như bản thẻ cũ).

- src/styles/app.css
  + .barChart, .barChart-col, .barChart-bar, .barChart-value,
    .barChart-label — biểu đồ cột TỰ VẼ BẰNG CSS THUẦN (không dùng
    thư viện ngoài) — có chủ đích: package.json chưa có thư viện biểu
    đồ nào, thêm mới tăng rủi ro (không cài/test được npm install
    thật trong sandbox lần này do bị reset) và tăng dung lượng bundle
    không cần thiết cho 1 biểu đồ cột đơn giản.
  + .tableScroll, .adminTable, .adminTable-row, .adminTable-name,
    .adminTable-sku, .adminTable-expandRow — style bảng dữ liệu.
  + .pctCell, .pctCell-bar, .pctCell-fill, .pctCell-num — thanh tiến
    trình mini trong ô % Tổng kho.
  + .stockStatus + .ok/.warn/.danger — badge trạng thái tồn kho (tên
    class MỚI, tách riêng khỏi .badge dùng cho trạng thái đếm hàng và
    .alertType-* dùng cho cảnh báo — tránh xung đột giữa 3 hệ thống
    badge khác domain trong cùng app).

ĐÃ TỰ KIỂM TRA KỸ (dựng hẳn môi trường test độc lập vì sandbox bị
reset, thiếu App.jsx/main.jsx/index.html/vite.config.js để chạy được
cả app thật qua Vite):
1. esbuild xác nhận cú pháp JSX + CSS hợp lệ.
2. Phát hiện CDN unpkg.com bị chặn trong sandbox (403) khi thử dựng
   harness test bằng React qua CDN — chuyển sang cài react/react-dom/
   @babel/standalone qua npm (registry chính thức, được phép) rồi
   dùng bản cài cục bộ — giải quyết đúng vấn đề thay vì bỏ cuộc.
3. Test bằng Playwright với dữ liệu giả lập (7 sản phẩm, đủ 3 trạng
   thái tồn kho) trên React THẬT (không phải HTML/CSS tĩnh suông):
   - Biểu đồ cột vẽ đúng tỉ lệ, nhãn dài tự cắt ellipsis không vỡ layout.
   - Bảng cuộn ngang đúng trên màn hẹp (390px, bảng rộng 640px) —
     xác nhận đủ cả 7 cột khi cuộn, % TÍNH ĐÚNG (42.2+25.5+25.5+5.8+
     0.5+0.4+0.0 ≈ 100%, khớp toán học).
   - Badge trạng thái đúng màu cho cả 3 mức, đúng logic ngưỡng.
   - Bấm 1 dòng MỞ RỘNG đúng, hiện đúng chi tiết lô của ĐÚNG sản phẩm.
   - Test desktop (950px) — đủ cột, giao diện cân đối.

LƯU Ý: môi trường test tự dựng chỉ trích đúng phần CSS mới (không có
media query responsive desktop mở rộng .page-main từ app.css thật) —
bản desktop test hẹp hơn thực tế sẽ chạy trong app thật (app thật đã
có sẵn media query mở rộng .page-main 880-1040px trên desktop, KHÔNG
đổi gì trong lần sửa này) — không phải lỗi, chỉ hạn chế môi trường
test rút gọn.

Việc cần làm: chạy `npm run build` thật trên máy bạn (sandbox thiếu
Vite đầy đủ nên chưa chạy được build thật lần này) để xác nhận build
production thành công trước khi deploy Railway/Vercel.
