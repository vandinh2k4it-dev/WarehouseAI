fix(frontend): gửi bù App.jsx + ImportFlow.jsx — sót từ commit trước

Bối cảnh lỗi:
Bạn báo không thấy nút "+ Quét phiếu mới" trên trang danh sách phiếu
nhập, dù mình đã báo tính năng quét phiếu (ScanReceipt.jsx) đã xong.

Nguyên nhân thật (lỗi đóng gói của mình, không phải lỗi code):
Khi xây tính năng quét phiếu, mình có sửa 3 file: ScanReceipt.jsx
(trang mới), App.jsx (đăng ký route /import/scan), và ImportFlow.jsx
(thêm nút "+ Quét phiếu mới" dẫn tới route đó). Nhưng lúc đó bạn yêu
cầu tạm dừng tính năng này để làm việc khác, nên mình CHƯA đóng gói
gửi cả 3 file — chỉ có ScanReceipt.jsx được gửi sau này (lẫn trong
đợt "4 tính năng nhỏ"), còn App.jsx và ImportFlow.jsx bị BỎ SÓT hoàn
toàn, chưa từng gửi cho bạn ở bất kỳ lần nào trước đó.

Kết quả: ScanReceipt.jsx nằm sẵn trong code bạn, nhưng KHÔNG CÓ ROUTE
nào trỏ tới nó (App.jsx chưa đăng ký) và KHÔNG CÓ NÚT nào để vào trang
đó (ImportFlow.jsx chưa có nút) — nên hoàn toàn không thể truy cập
được dù code đã có sẵn.

File trong lần gửi bù này:
- src/App.jsx — đăng ký route "/import/scan" -> ScanReceipt
- src/pages/ImportFlow.jsx — thêm nút "+ Quét phiếu mới" ở đầu danh
  sách phiếu, dẫn tới /import/scan

Đã build lại (npm run build) xác nhận không lỗi trước khi gửi.
