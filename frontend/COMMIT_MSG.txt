feat(dashboard): thêm Dashboard báo cáo/phân tích — Nhóm B (mục an toàn nhất)

Chỉ chọn 1 hạng mục an toàn nhất trong Nhóm B (đã trao đổi trước) — KHÔNG
đổi schema database, KHÔNG đụng tới luồng nhập/xuất/đếm camera đang chạy
ổn định. Tận dụng lại đúng dữ liệu InventoryTransaction/Inventory/Alert
đã có sẵn từ đầu.

FRONTEND:
- api.js: thêm getAnalytics(days).
- pages/Overview.jsx: thêm 2 card mới vào đúng trang "Tổng quan" đã có
  sẵn — "Xu hướng nhập-xuất" (biểu đồ cột CSS thuần theo từng ngày) và
  "Top sản phẩm quay vòng nhanh" (danh sách có thanh ngang tỉ lệ).
  KHÔNG thêm route/tab điều hướng mới — bottom nav mobile đã được tinh
  chỉnh đúng cho 6 mục (xem comment CSS gốc), thêm mục thứ 7 sẽ làm chật
  giao diện trên điện thoại thật, đi ngược mục tiêu làm mượt UX.
  Lời gọi getAnalytics() được TÁCH RIÊNG khỏi Promise.all() chính — nếu
  endpoint mới lỗi vì bất kỳ lý do gì, chỉ 2 card thống kê bị ẩn, không
  làm hỏng cả trang Tổng quan (4 stat card + cảnh báo + phiếu gần đây)
  vốn đã chạy ổn định từ trước.
- styles/app.css: thêm CSS cho .flowChart/.topProductList — dùng lại
  đúng biến màu --success/--accent/--info-dim đã có sẵn, KHÔNG thêm thư
  viện biểu đồ ngoài (recharts/chart.js...) để tránh rủi ro npm install
  sát ngày bảo vệ.
