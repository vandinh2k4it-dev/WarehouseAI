feat(frontend): thanh trượt chỉnh độ nhạy cho chế độ Quay video (không chỉ Trực tiếp)

Nối vào backend đã có sẵn tham số conf trên /count-video (xem
COMMIT_MSG.txt bên backend cùng đợt).

File sửa:
- src/api.js
  countVideo() thêm tham số thứ 4 "conf" (mặc định 0.5) — gửi thêm
  field "conf" vào FormData cùng file video và threshold_pct đã có.
  GIỮ NGUYÊN vị trí tham số thresholdPct ở vị trí thứ 3 (không đổi thứ
  tự tham số cũ, tránh phá vỡ chỗ gọi khác nếu có — chỉ thêm tham số
  MỚI vào cuối).

- src/components/CountingScreen.jsx
  + Thêm state videoConf (mặc định 0.5, khớp đúng mặc định mới bên
    backend).
  + Thêm thanh trượt "Độ nhạy phát hiện" (0.15-0.8) ngay phía TRÊN 2
    nút "Quay video mới"/"Chọn video có sẵn" khi đang ở chế độ video —
    kèm chú thích ngắn giải thích kéo bên nào khi nào, VÀ lưu ý rõ
    ràng tính năng này không loại được hết mọi trường hợp nhận nhầm
    (tránh người dùng kỳ vọng sai).
  + Sửa lời gọi api.countVideo() để truyền đúng videoConf vào ĐÚNG VỊ
    TRÍ tham số thứ 4 (giữ nguyên threshold_pct mặc định 0.02 ở vị trí
    thứ 3) — LỖI THẬT ĐÃ TỰ PHÁT HIỆN VÀ SỬA TRƯỚC KHI GIAO: lúc đầu
    viết nhầm api.countVideo(session.id, file, videoConf) — đưa
    videoConf vào ĐÚNG VỊ TRÍ của thresholdPct (tham số thứ 3), nghĩa
    là ngưỡng đối chiếu khớp/lệch (threshold_pct) sẽ bị ghi đè bằng
    giá trị confidence (vd 0.5) thay vì giữ đúng 0.02 mặc định — lỗi
    này sẽ làm sai lệch tiêu chí đối chiếu khớp/lệch một cách ÂM THẦM,
    khó phát hiện qua test thông thường vì không gây crash, chỉ cho
    kết quả đối chiếu sai lặng lẽ. Đã tự nhận ra khi đọc lại đúng chữ
    ký hàm thật trong api.js (không giả định), sửa lại gọi đúng thứ
    tự: countVideo(session.id, file, 0.02, videoConf).

ĐÃ TỰ KIỂM TRA (dùng esbuild build thử — không có sẵn Vite/npm đầy đủ
trong sandbox lần này do bị reset, dùng esbuild có sẵn để xác nhận cú
pháp hợp lệ thay vì chỉ đọc bằng mắt):
- api.js: build thành công qua esbuild (chỉ 1 cảnh báo về import.meta
  không tương thích định dạng iife khi build ngoài Vite — không phải
  lỗi cú pháp, chỉ là hạn chế môi trường test, Vite thật sẽ xử lý đúng
  import.meta bình thường).
- CountingScreen.jsx: build JSX thành công qua esbuild (bundle, đánh
  dấu external cho react/LiveCountingScreen/api để tránh lỗi thiếu
  file phụ thuộc không liên quan tới việc kiểm tra cú pháp).
- Đếm thủ công số thẻ <div> mở/đóng: xác nhận cân bằng đúng (bản gốc
  16 mở/15 đóng do có 1 div tự đóng <div className="spinner" />; bản
  đã sửa 19 mở/18 đóng — đúng +3 cặp mới thêm, không lệch).

CHƯA làm được trong lần này (thiếu Vite/npm trong sandbox bị reset):
chưa chạy được npm run build thật để build ra dist/ hoàn chỉnh như
mọi lần trước — chỉ xác nhận được CÚ PHÁP đúng qua esbuild, chưa chạy
được toàn bộ pipeline build + test Playwright như quy trình chuẩn đã
dùng xuyên suốt trước đó. Đề nghị bạn tự chạy npm run build trên máy
mình sau khi áp dụng để chắc chắn 100% trước khi deploy Railway/Vercel.
